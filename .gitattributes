# Shell scripts must stay LF: CRLF corrupts every line a shell parses, not
# just the shebang, and the backend Docker entrypoint depends on this file
# running correctly on container start.
*.sh text eol=lf
