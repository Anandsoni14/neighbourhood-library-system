# Loaded automatically by Vitest (Vite resolves env files for `mode: 'test'`).
# Checked into git — unlike .env, tests must not depend on a developer's local
# untracked env file, or they silently break in CI where no .env exists.
VITE_API_BASE_URL=/api/v1
