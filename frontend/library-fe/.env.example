VITE_API_BASE_URL=/api/v1
# Read by vite.config.ts (Node process.env, not exposed to client code) to pick
# the dev-server proxy target. Defaults to http://localhost:8000 if unset.
VITE_API_PROXY_TARGET=http://localhost:8000
