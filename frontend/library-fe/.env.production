VITE_API_BASE_URL=/api/v1
# Checked into git so `vite build` (production mode) has this even when a
# developer's untracked .env is absent — e.g. in CI, where the e2e suite
# builds and serves the production bundle and needs requests to hit
# /api/v1/* to match its route mocks.
