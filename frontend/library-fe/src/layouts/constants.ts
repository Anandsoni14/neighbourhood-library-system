// Shared between Sidebar (its own width) and AppLayout (offsetting the
// content area) — kept in one place so they can't drift out of sync.
export const DRAWER_WIDTH = 220;
export const APP_BAR_HEIGHT = 48;
